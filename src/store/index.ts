import { defineStore, storeToRefs } from "pinia";
import { threadId } from "worker_threads";

//命名按照use--Xxx--Store的格式
export const useIndexStore = defineStore('index', {
  state: () => {
    return {

    }
  },
  getters: {
  },
  actions: {
  }
})